/**
 * Global Feature Flags
 * 
 * Toggle experimental or trial features across the application.
 */

// Consistency Score Trial Feature (1-Month Trial)
// Set to false to immediately disable all Consistency Score UI rendering app-wide.
export const FEATURE_CONSISTENCY_SCORE = true;

// Optional 1-Tap Energy Check-in Trial Feature (1-Month Trial)
// Set to false to immediately disable Energy Check-in UI and difficulty nudges app-wide.
export const FEATURE_ENERGY_CHECKIN = true;
